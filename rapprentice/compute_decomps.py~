import h5py
import numpy as np
import argparse

def parse_arguments():
    parser = argparse.ArgumentParser()
    parser.add_argument('datafile', type=str)
    parser.add_argument('bend_coeff_init', type=float)
    parser.add_argument('bend_coeff_final', type=float)
